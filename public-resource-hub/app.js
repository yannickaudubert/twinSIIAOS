const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
let state={view:'overview',q:'',domain:'all',authority:'all',sort:'authority'};
const rank=a=>a==='S'?4:a==='S/A'?3:a==='A/S'?3:a==='A'?2:a==='A/B'?1:0;
function layout(){document.querySelector('#app').innerHTML=`<div class="shell"><header class="topbar"><div class="brand"><div class="mark">S</div><div>SIIAOS RESOURCE RADAR<small>AgorIA · digital commons / hyperveille</small></div></div><nav class="nav"><button data-view="overview" class="active">Overview</button><button data-view="explorer">Explorer</button><button data-view="watch">Hyperveille</button><button data-view="map">Dependency Map</button></nav><button class="ghost" id="lang">FR · EN</button></header><main><section id="overview" class="view active"></section><section id="explorer" class="view"></section><section id="watch" class="view"></section><section id="map" class="view"></section></main><footer class="footer"><span>AgorIA · SIIAOS Digital Commons — public demonstrator</span><span>External sources remain upstreams. The governed local registry is the system of record.</span></footer></div><aside class="drawer" id="drawer"></aside>`}
function hero(){const domains=new Set(SOURCES.map(x=>x.domain)).size;return `<div class="hero"><article class="hero-card hero-main"><span class="eyebrow">◉ Public commons · local-first intelligence</span><h1>One radar.<br><span class="gradient">Every AI ecosystem.</span></h1><p class="lead">A public discovery layer for models, datasets, papers, launchers, runtimes and specialist repositories. The SIIAOS does not become dependent on them: agents qualify what matters, preserve provenance, and prepare governed local reuse.</p><div class="hero-actions"><button class="primary" data-go="explorer">Explore ${SOURCES.length} sources</button><button class="ghost" data-go="watch">See the agentic pipeline</button></div></article><aside class="hero-card hero-side"><div><span class="kicker">RESOURCE INTELLIGENCE</span><h2>From Web signal to local capacity.</h2><p class="muted">Every upstream is treated as replaceable. Knowledge accumulated locally is not.</p></div><div class="metric-grid"><div class="metric"><b>${SOURCES.length}</b><span>curated sources</span></div><div class="metric"><b>${domains}</b><span>domains</span></div><div class="metric"><b>${AGENTS.length}</b><span>agent roles</span></div><div class="metric"><b>∞</b><span>extensible adapters</span></div></div><div class="pipeline"><div class="pipe"><i class="dot"></i> Discover external signals</div><div class="pipe"><i class="dot"></i> Verify licence, security, provenance</div><div class="pipe"><i class="dot"></i> Benchmark against local capabilities</div><div class="pipe"><i class="dot"></i> Mirror, integrate, document, watch</div></div></aside></div>`}
function card(x){return `<article class="card" data-id="${x.id}"><div class="card-head"><div><div class="small muted">${x.kind}</div><h3>${x.name}</h3></div><b>${x.authority}</b></div><div class="tagrow"><span class="tag">${x.domain}</span><span class="tag">${x.local}</span></div><p class="small muted">${x.role}</p><div class="card-head small"><span class="stars">${'★'.repeat(x.volume)}${'☆'.repeat(5-x.volume)}</span><span>${x.owner}</span></div></article>`}
function overview(){const top=[...SOURCES].sort((a,b)=>rank(b.authority)-rank(a.authority)||b.volume-a.volume).slice(0,9);$('#overview').innerHTML=hero()+`<div class="section-title"><div><span class="kicker">CURATED UPSTREAMS</span><h2>The public resource constellation</h2></div><button class="ghost" data-go="explorer">Browse all</button></div><div class="grid">${top.map(card).join('')}</div><div class="section-title"><div><span class="kicker">GOVERNED LOCAL-FIRST</span><h2>What the SIIAOS keeps</h2></div></div><div class="flow"><span>URL + provenance</span><span>licence</span><span>hash / version</span><span>dependencies</span><span>benchmark</span><span>decision</span><span>local package</span><span>RETEX</span></div><div class="notice">The aggregator is not a proxy for blindly installing resources. Production mode requires explicit adapters, validation policies and a governed local registry before resources become executable capabilities.</div>`}
function explorer(){const domains=['all',...new Set(SOURCES.map(x=>x.domain))];$('#explorer').innerHTML=`<div class="section-title"><div><span class="kicker">EXPLORER</span><h2>Search across ecosystems</h2></div><span class="muted small">Authority = institutional/maturity signal · Volume = relative catalog depth</span></div><div class="toolbar"><input id="q" class="input" placeholder="Search source, owner, role, risk…"><select id="domain" class="select">${domains.map(x=>`<option value="${x}">${x==='all'?'All domains':x}</option>`).join('')}</select><select id="authority" class="select"><option value="all">All authority levels</option><option>S</option><option>S/A</option><option>A/S</option><option>A</option><option>A/B</option></select><select id="sort" class="select"><option value="authority">Authority first</option><option value="volume">Volume first</option><option value="name">Name A–Z</option></select></div><div class="grid" id="results"></div>`;renderResults();$('#q').addEventListener('input',e=>{state.q=e.target.value;renderResults()});$('#domain').addEventListener('change',e=>{state.domain=e.target.value;renderResults()});$('#authority').addEventListener('change',e=>{state.authority=e.target.value;renderResults()});$('#sort').addEventListener('change',e=>{state.sort=e.target.value;renderResults()})}
function renderResults(){let a=SOURCES.filter(x=>(state.domain==='all'||x.domain===state.domain)&&(state.authority==='all'||x.authority===state.authority)&&JSON.stringify(x).toLowerCase().includes(state.q.toLowerCase()));a.sort(state.sort==='name'?(a,b)=>a.name.localeCompare(b.name):state.sort==='volume'?(a,b)=>b.volume-a.volume:(a,b)=>rank(b.authority)-rank(a.authority)||b.volume-a.volume);$('#results').innerHTML=a.length?a.map(card).join(''):`<div class="empty">No source matches these filters.</div>`}
function watch(){$('#watch').innerHTML=`<div class="section-title"><div><span class="kicker">HYPERVEILLE</span><h2>Eight roles turn change into governed capacity</h2></div></div><div class="flow"><span>Internet / feeds</span><span>signal capture</span><span>qualification</span><span>security & licence</span><span>benchmark</span><span>human decision</span><span>local integration</span><span>continuous watch</span></div><div class="agents">${AGENTS.map(a=>`<article class="agent"><div class="agent-icon">${a.icon}</div><h3>${a.name}</h3><p class="muted small">${a.mission}</p><b class="small">Output · ${a.output}</b></article>`).join('')}</div><div class="section-title"><div><span class="kicker">DECISION BOUNDARY</span><h2>Proposal-first, proof-first</h2></div></div><div class="notice">Agents may discover, classify, compare and prepare. Sensitive installation, publication or execution remains governed by policy and human validation. The demonstration visualizes the workflow; it does not claim autonomous production ingestion.</div>`}
function map(){const groups={Hubs:['Hugging Face','ModelScope','Kaggle Models','OpenCSG','NVIDIA NGC'],Specialists:['Civitai','Tensor.Art','OpenMMLab','MONAI Model Zoo','TorchGeo'],Launchers:['Pinokio','Stability Matrix','Comfy Registry'],Local:['Ollama','LM Studio','LocalAI','Jan','Open WebUI']};$('#map').innerHTML=`<div class="section-title"><div><span class="kicker">DEPENDENCY MAP</span><h2>External upstreams → governed local commons</h2></div></div><div class="map"><div class="mapcol"><h3>External sources</h3>${Object.entries(groups).map(([k,v])=>`<div class="small muted">${k}</div>${v.map(n=>`<div class="node">${n}</div>`).join('')}`).join('')}</div><div class="mapcol core"><h3>SIIAOS Resource Intelligence Layer</h3><div class="node">Source adapters & scheduled discovery</div><div class="node">Canonical metadata + provenance</div><div class="node">Licence & policy checks</div><div class="node">Supply-chain/security analysis</div><div class="node">Benchmarks & compatibility profiles</div><div class="node">Human validation / mandates</div><div class="node">Local registry + mirror + knowledge base</div></div><div class="mapcol"><h3>Local capabilities</h3><div class="node">Models & embeddings</div><div class="node">Datasets & corpora</div><div class="node">Workflows & applications</div><div class="node">MCP servers & tools</div><div class="node">Agents & skills</div><div class="node">Docs, decisions & RETEX</div><div class="node">User-specific SIIAOS runtime</div></div></div>`}
function detail(id){const x=SOURCES.find(x=>x.id===id);if(!x)return;const d=$('#drawer');d.innerHTML=`<button class="close">✕</button><span class="kicker">${x.kind}</span><h2>${x.name}</h2><p class="lead" style="font-size:15px">${x.role}</p><dl class="detail"><dt>Domain</dt><dd>${x.domain}</dd><dt>Authority</dt><dd>${x.authority}</dd><dt>Relative volume</dt><dd class="stars">${'★'.repeat(x.volume)}${'☆'.repeat(5-x.volume)}</dd><dt>Actor / owner</dt><dd>${x.owner}</dd><dt>Depends on</dt><dd>${x.depends}</dd><dt>Local readiness</dt><dd>${x.local}</dd><dt>Watch point</dt><dd>${x.risk}</dd></dl><p><a class="primary" style="display:inline-block;text-decoration:none" href="${x.url}" target="_blank" rel="noopener noreferrer">Open official source ↗</a></p><div class="notice small">SIIAOS policy: external content is a signal until provenance, licence, security and compatibility have been checked.</div>`;d.classList.add('open');$('.close',d).onclick=()=>d.classList.remove('open')}
function setView(v){state.view=v;$$('.view').forEach(e=>e.classList.toggle('active',e.id===v));$$('.nav button').forEach(e=>e.classList.toggle('active',e.dataset.view===v));if(v==='explorer'&&!$('#results'))explorer();window.scrollTo({top:0,behavior:'smooth'})}
function events(){document.addEventListener('click',e=>{const c=e.target.closest('[data-id]');if(c)detail(c.dataset.id);const go=e.target.closest('[data-go]');if(go)setView(go.dataset.go);const nav=e.target.closest('[data-view]');if(nav)setView(nav.dataset.view)});document.addEventListener('keydown',e=>{if(e.key==='Escape')$('#drawer').classList.remove('open')})}
layout();overview();explorer();watch();map();events();